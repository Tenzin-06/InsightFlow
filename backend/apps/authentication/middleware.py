# Clerk middleware removed — authentication is now handled entirely by
# JWTAuthentication (apps.authentication.authentication.JWTAuthentication)
# via Django REST Framework's DEFAULT_AUTHENTICATION_CLASSES setting.
